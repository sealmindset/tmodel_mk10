<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>LLM Provider Settings</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
  <%- include('./partials/header.ejs') %>
  <div class="container mt-4">
    <h1>LLM Provider Settings</h1>
    <form id="llm-provider-form" action="/settings/llmprovider" method="POST">
      <div class="form-check">
        <input class="form-check-input" type="radio" name="llmProvider" id="openai" value="openai" <%= settings['settings:llm:provider'] === 'openai' ? 'checked' : '' %>>
        <label class="form-check-label" for="openai">OpenAI</label>
      </div>
      <div class="form-check">
        <input class="form-check-input" type="radio" name="llmProvider" id="ollama" value="ollama" <%= settings['settings:llm:provider'] === 'ollama' ? 'checked' : '' %>>
        <label class="form-check-label" for="ollama">Ollama</label>
      </div>
      <button type="submit" class="btn btn-primary mt-2">Save LLM Provider</button>
    </form>
    <% if (message) { %>
      <div class="alert alert-<%= message.type %> mt-3"> <%= message.text %> </div>
    <% } %>
  </div>
</body>
</html>
